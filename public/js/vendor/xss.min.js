// Stub: filterXSS is normally provided by xss.js library.
// In standalone mode, we provide a passthrough to prevent ReferenceErrors
// in functionsfirst.js/sdk.js cleanData(). The full Bastyon platform
// provides the real xss.min.js with proper sanitization.
window.filterXSS = function(str) { return typeof str === "string" ? str : ""; };
